type Instrument = string
let primaryInstrument: Instrument = 'guitar'

type Vehicle = {
    model: string
    year: number
    isElectric: boolean
}

let vehicle: Vehicle = {
    model: 'Civic',
    year: 2021,
    isElectric: false
}

let vehicle2: Vehicle = {
    model: 'Model3',
    year: 2023,
    isElectric: true
}